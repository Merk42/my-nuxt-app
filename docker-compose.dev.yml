services:
  my-app-dev:
    build:
      context: .
      dockerfile: ./Dockerfile.dev
    ports:
      - "3000:3000"
      - "24678:24678"
    volumes:
      - .:/src
      - /src/node_modules
    working_dir: /src
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - NODE_ENV=development